<div class="container gallery-edit-container">
    <h1>Editing <%=@grade.level.ordinalize + " Grade"%></h1>

    <div class="row">
        <%= render 'form', grade: @grade %>
    </div>

    <% if @grade.id != nil %>

    <div class="row">
         <h3>Image Uploader</h3>
    </div>

    <div class="row">
        <%= render 'image_form', image: @image %>
    </div>

    <% end %>
    <div class="row">
        <div class="gallery-back-btn">
            <%= link_to 'Back', admin_path %>
        </div>
    </div>
</div>
<br>